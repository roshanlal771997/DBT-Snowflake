version: 2

sources:
  - name: demo
    database: DBT_PROJECT_DB
    schema: DEMO_SCHEMA
    tables:
      - name: customers
        columns:
          - name: customer_id
            tests:
              - not_null
              - unique
      - name: orders
        columns:
          - name: order_id
            tests:
              - not_null
              - unique
